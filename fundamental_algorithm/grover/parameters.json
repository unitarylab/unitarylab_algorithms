{
    "title": "Grover",
    "description": "Grover search algorithm is used to find specific entries in an unsorted database, providing quadratic speedup compared to classical algorithms.",
    "tags": [
        "Quantum Search",
        "Amplitude Amplification",
        "Unsorted Database"
    ],
    "tab": "Search",
    "params": [
        {
            "max": 10,
            "min": 1,
            "help": "The number of qubits in the data register.",
            "name": "n",
            "value": "3",
            "pattern": "^[1-9]\\d*$",
            "description": "Number of qubits",
            "info": "请输入正整数，范围在1到10之间（如 3）"
        },
        {
            "help": "The target state to search for (binary string, e.g., '101'). Length should equal the number of qubits.",
            "name": "target",
            "value": "101",
            "pattern": "^[01]+$",
            "description": "Target state",
            "info": "请输入01字符串，长度应等于n（示例：101）"
        }
    ]
}
